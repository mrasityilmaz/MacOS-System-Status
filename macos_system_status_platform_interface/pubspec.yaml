name: macos_system_status_platform_interface
description: A common platform interface for the macos_system_status plugin.
repository: https://github.com/mrasityilmaz/MacOS-System-Status/tree/main/macos_system_status_platform_interface
version: 1.0.0
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


environment:
  sdk: '>=3.5.0-218.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.0
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0



dev_dependencies:
  build_runner: ^2.4.11
  flutter_test:
    sdk: flutter
  freezed: ^2.5.3
  json_serializable: ^6.8.0
  very_good_analysis: ^6.0.0
